---
'@novastarter/database-driver-postgres': minor
---

Add `DatabaseDriverPostgres`, a node-postgres driver for `@novastarter/database`: a `pg` pool from a URL, pool options or a pool of the caller's own, Drizzle's `NodePgDatabase` with optional query logging, and `migrate()` over drizzle-kit folders.
