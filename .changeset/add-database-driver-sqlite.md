---
'@novastarter/database-driver-sqlite': minor
---

Add `DatabaseDriverSqlite`, a better-sqlite3 driver for `@novastarter/database`: a file or `:memory:` database with foreign keys on and WAL for files, Drizzle's `BetterSQLite3Database`, and `migrate()` over drizzle-kit folders.
