---
'@novastarter/database-driver-supabase': minor
---

Add `DatabaseDriverSupabase`, Supabase Postgres for `@novastarter/database` on the postgres driver: the dashboard's connection string (direct host, session or transaction pooler) with TLS verified by default and the project's root certificate through `ca`.
