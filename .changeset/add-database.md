---
'@novastarter/database': minor
---

Add `@novastarter/database`: the `DatabaseDriver` contract over Drizzle ORM (`db`, `ping()`, `migrate()`, `close()`), the `DatabaseManager` of `useDatabase()` mapping named locations to drivers, and typed `location()` results through the augmentable `DatabaseLocations` map.
