---
'@novastarter/emitter': minor
'@novastarter/types': minor
---

Add `@novastarter/emitter` with `useEmitter` and the `Emitter` class carrying filter, action and init hooks, and the `EventContext`, `FilterHandler`, `ActionHandler` and `InitHandler` types in `@novastarter/types`.
