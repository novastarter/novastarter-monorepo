---
'@novastarter/env': minor
'@novastarter/constants': minor
'@novastarter/utils': minor
---

Add `@novastarter/env` (`useEnv`) for reading configuration from `process.env` and `.env`/JSON/YAML/JS config files with `_FILE` secrets and `type:` cast prefixes, plus `@novastarter/constants` and the `toArray`, `toBoolean`, `isIn`, `parseJSON`, `requireYaml` helpers in `@novastarter/utils` it depends on.
