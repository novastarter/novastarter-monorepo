---
'@novastarter/logger': minor
'@novastarter/env': minor
'@novastarter/constants': minor
---

Add `@novastarter/logger` with `useLogger`, `createLogger`, `createHttpLogger` and the bus-backed `LogsStream`, `getConfigFromEnv` in `@novastarter/env` and `REDACTED_TEXT` in `@novastarter/constants`.
