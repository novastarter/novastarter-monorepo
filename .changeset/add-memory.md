---
'@novastarter/memory': minor
'@novastarter/errors': minor
'@novastarter/utils': minor
'@novastarter/types': minor
---

Add `@novastarter/memory` with `createKv`, `createCache`, `createBus` and `createLimiter` (local and Redis backends), `@novastarter/errors` with `createError`, `isNovastarterError` and `HitRateLimitError`, `processId` in `@novastarter/utils/node` and the `NovastarterError` type.
