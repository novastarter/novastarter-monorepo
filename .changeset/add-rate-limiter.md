---
'@novastarter/rate-limiter': minor
---

Add `RateLimiter` and the `handleRateLimit` Express middleware for rejecting requests when event loop delay, event loop utilization or memory usage exceed configured limits.
