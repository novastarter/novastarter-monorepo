---
'@novastarter/release-notes-generator': minor
---

Add `@novastarter/release-notes-generator`, a changesets changelog plugin that prints grouped release notes with commit links (and GitHub Actions outputs) on `pnpm changeset version` instead of writing per-package `CHANGELOG.md` files; no GitHub token is needed.
