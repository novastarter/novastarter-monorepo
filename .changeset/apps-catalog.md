---
'web': patch
'docs': patch
---

The Next.js apps take every dependency version from the workspace catalogs (`catalog:` and `catalog:apps` for TypeScript and `@types/node`) instead of inline versions.
