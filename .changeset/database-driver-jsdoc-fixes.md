---
'@novastarter/database': patch
'@novastarter/database-driver-pglite': patch
'@novastarter/database-driver-turso': patch
---

Documentation fixes: the errors barrel carries the package header again, the exported `dataDirectory` and `localFilePath` helpers show a usage example, and the Turso driver documents the filesystem error its constructor can throw.
