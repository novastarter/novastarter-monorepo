---
'docs': patch
---

The app-router modules and the Next.js configuration are documented per the comment rules, and the TypeScript configuration defers to the shared presets instead of restating them.
