---
'@novastarter/payments-driver-polar': patch
'@novastarter/payments-driver-paddle': patch
'@novastarter/payments-driver-lemonsqueezy': patch
---

The readmes describe the `priceId` of a checkout as the id the application's plans record for the driver, rather than as a `providerIds` entry of the plan catalog that `@novastarter/payments` no longer ships.
