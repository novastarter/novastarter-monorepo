---
'@novastarter/feature-flags': patch
---

`FeatureFlags` is now exported from the package entry, and the readme and `FeatureFlagsDriverStatic` example show `registerFeatureFlags({ flags })` / `useFeatureFlags().get()` instead of the removed locations.
