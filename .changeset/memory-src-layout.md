---
'@novastarter/memory': patch
---

`@novastarter/memory` keeps each of its four subsystems in the `driver.ts` / `types.ts` / `lib/drivers/` / `lib/<name>-manager.ts` / `lib/use-<name>.ts` layout of the other driver-based packages; every export of the package is unchanged.
