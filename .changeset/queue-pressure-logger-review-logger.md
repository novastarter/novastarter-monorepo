---
'@novastarter/logger': patch
---

`LogsStream` identifies the process with `processId()` of `@novastarter/utils`, the same origin id the multi-stage cache stamps; the `nanoid` dependency is removed.
