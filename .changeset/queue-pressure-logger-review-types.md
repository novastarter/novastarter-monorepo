---
'@novastarter/types': patch
---

Splits the type tests into one file per module: `error`, `events` and `filter` are each covered by their own test now, and `index` keeps the checks of the package entry point.
