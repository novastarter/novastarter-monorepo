---
'@novastarter/emitter': patch
---

The `onFilter` examples in the README and the `Emitter` JSDoc now pass a payload type argument, so they compile as written; code comments were completed with no behaviour change.
