---
'@novastarter/mail-driver-mailgun': patch
---

Mailgun driver: comments and tests only, no behaviour change — the message mapper now has its own test file and the driver's send/verify steps are fully commented.
