---
'@novastarter/mail-driver-mailjet': patch
---

The Mailjet message mapper now has its own test file next to `to-mailjet-message.ts` and the SDK mock in the driver test is documented; no runtime behaviour changed.
