---
'@novastarter/mail-driver-sendgrid': patch
---

Test-only change: the SendGrid message mapper now has its own `to-sendgrid-mail.test.ts` and the SDK mock in the driver tests is documented; nothing changes for callers.
