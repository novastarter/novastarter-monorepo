---
'@novastarter/push-driver-apns': patch
---

The APNs driver's send-deadline option is renamed from `requestTimeout` to `timeout`, matching the other push and SMS drivers; rename the key in the location's `options`.
