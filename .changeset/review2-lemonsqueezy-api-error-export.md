---
'@novastarter/payments-driver-lemonsqueezy': minor
---

`LemonSqueezyApiError` is now exported from the package entry: the refusal every public method documents `@throws` for can be classified with `instanceof` from the package root instead of reaching into the driver's internals.
