---
'@novastarter/logger': patch
---

`getLogsStream` and `getHttpLogsStream` were renamed to `useLogsStream` and `useHttpLogsStream`; update the calls — the streams and their arguments are unchanged.
