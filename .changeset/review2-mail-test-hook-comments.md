---
'@novastarter/mail': patch
---

Mail test suites: the `beforeEach`/`afterEach` hooks of `read-attachment.test.ts` and `send-mail.test.ts` carry the kit's numbered block comments like every other function body.
