---
'@novastarter/mail-driver-mailtrap': patch
---

Mailtrap mapper: the numbered block comments inside `toMailtrapMail` run sequentially through the whole function, the tag-trimming while loop included; no behavior change.
