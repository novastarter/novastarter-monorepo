---
'@novastarter/payments': patch
'@novastarter/payments-driver-stripe': patch
'@novastarter/payments-driver-paddle': patch
'@novastarter/payments-driver-lemonsqueezy': patch
---

Comment-only: numbered block comments now cover the first logical block of `toSubscription` in the Paddle and Lemon Squeezy drivers and the one-expression helpers `usePayments` and the Stripe driver's `fromUnix`, so every logical block of a function body is numbered with no gaps.
