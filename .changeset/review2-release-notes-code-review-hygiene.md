---
'@novastarter/release-notes-generator': patch
---

The inline step comments of `bumpPackage` are numbered sequentially and `generateMarkdown` compares the change count with strict equality; no behavior change.
