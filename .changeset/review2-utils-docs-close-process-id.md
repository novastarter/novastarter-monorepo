---
'@novastarter/utils': patch
---

No behaviour change: the `LocationManager.close()` docs now state the release-snapshot guarantee exactly, and the `processId()` docs say the id hashes the time of the first call.
