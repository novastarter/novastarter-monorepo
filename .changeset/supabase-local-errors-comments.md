---
'@novastarter/storage-driver-supabase': minor
'@novastarter/storage-driver-local': minor
---

A missing or wrong driver option now throws `InvalidConfigError` (`INVALID_CONFIG`) instead of a plain `Error`.
