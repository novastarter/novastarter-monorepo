---
'@novastarter/errors': patch
'@novastarter/emitter': patch
'@novastarter/validation': patch
---

`@novastarter/types` is a regular dependency of `@novastarter/errors`, `@novastarter/emitter` and `@novastarter/validation`, so their type declarations resolve without installing it yourself.
