/**
 * The Next.js configuration of the docs app: the defaults do everything this app needs, so the object stays empty —
 * unlike the web app it ships no Node-only dependencies the bundler must leave external.
 *
 * @type {import('next').NextConfig}
 */
const nextConfig = {};

/**
 * The configuration Next.js loads: the empty defaults of {@link nextConfig}.
 *
 * @type {import('next').NextConfig}
 */
export default nextConfig;
