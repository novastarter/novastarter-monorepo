{
	"name": "@novastarter/auth-driver-credentials",
	"version": "0.1.0",
	"description": "Password sign-in driver for `@novastarter/auth`",
	"repository": {
		"type": "git",
		"url": "https://github.com/novastarter/novastarter-monorepo.git",
		"directory": "packages/auth-driver-credentials"
	},
	"sideEffects": false,
	"type": "module",
	"exports": {
		".": "./dist/index.js",
		"./package.json": "./package.json"
	},
	"main": "dist/index.js",
	"files": [
		"dist"
	],
	"scripts": {
		"build": "tsdown src/index.ts --dts",
		"dev": "tsdown src/index.ts --dts --watch",
		"check-types": "tsc --noEmit -p tsconfig.json",
		"test": "vitest run",
		"test:coverage": "vitest run --coverage"
	},
	"dependencies": {
		"@novastarter/auth": "workspace:*",
		"@novastarter/errors": "workspace:*",
		"@novastarter/logger": "workspace:*",
		"@novastarter/utils": "workspace:*"
	},
	"devDependencies": {
		"@novastarter/tsconfig": "workspace:*",
		"@types/node": "catalog:",
		"@vitest/coverage-v8": "catalog:",
		"tsdown": "catalog:",
		"typescript": "catalog:",
		"vitest": "catalog:"
	}
}
