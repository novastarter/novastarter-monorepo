/**
 * Public entry point of `@novastarter/auth-driver-credentials`: the {@link AuthDriverCredentials} class and its
 * options.
 */
export { AuthDriverCredentials, type AuthDriverCredentialsConfig, type CredentialsUser } from './lib/driver.js';
