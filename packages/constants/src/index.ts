/**
 * Public entry point of `@novastarter/constants`.
 *
 * Constants are grouped per topic in their own module and re-exported here, so a consumer imports everything from the
 * package root while the source stays easy to navigate.
 */
export { DEFAULT_CHUNK_SIZE, JAVASCRIPT_FILE_EXTS } from './files.js';
export { REDACTED_TEXT } from './logging.js';
