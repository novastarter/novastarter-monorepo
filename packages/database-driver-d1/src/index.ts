/**
 * Public entry point of `@novastarter/database-driver-d1`: the {@link DatabaseDriverD1} class, its options and the
 * default export for consumers that import the driver without a named binding.
 */
import { DatabaseDriverD1 } from './lib/driver.js';

export { DatabaseDriverD1, type DatabaseDriverD1Config } from './lib/driver.js';
export default DatabaseDriverD1;
