/**
 * Entry point of `@novastarter/rate-limiter`.
 *
 * Exposes the {@link RateLimiter} class for standalone use and the {@link handleRateLimit} Express middleware
 * built on top of it.
 */
export * from './monitor.js';
export * from './express.js';
